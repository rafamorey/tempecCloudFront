//  navigation trow the app

signInButton.addEventListener('click', () => {
    location.hash = '#dashboard'
})

window.addEventListener('DOMContentLoaded', navigator, false)
window.addEventListener('hashchange', navigator, false)